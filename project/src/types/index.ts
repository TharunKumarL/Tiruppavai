export interface TiruppavaiDay {
  title: string;
  verses: string[];
  date: string;
  description?: string;
  meaning: string;
}

export interface GodaDeviImage {
  url: string;
  alt: string;
  caption?: string;
}